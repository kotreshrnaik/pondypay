import React from "react";
// import { Carousel, Image } from "react-bootstrap";
// import Slide1 from "../../Images/slider/Front-Page.png";

const MainSlider = () => {
  // const slides = [
  //   {
  //     id: 1,
  //     imgURL: Slide1,
  //     alt: "Image"
  //   },
  // ];
  return (
    <div>
      {/* <Carousel>
        {slides.map((slide) => (
          <Carousel.Item key={slide.id} interval={1000}>
            <Image
              className="d-block w-100"
              src={slide.imgURL}
              alt={slide.alt}
            />
          </Carousel.Item>
        ))}
      </Carousel> */}
    </div>
  );
};

export default MainSlider;
